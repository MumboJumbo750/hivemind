[pytest]
# Root-level pytest configuration for scripts/ tests.
# Used when running from /workspace (i.e. make health-test).
# Backend-specific configuration (asyncio, testpaths …) lives in /app/pytest.ini.
# PYTHONPATH=/workspace:/app is set by the make health-test target.
minversion = 7.0
